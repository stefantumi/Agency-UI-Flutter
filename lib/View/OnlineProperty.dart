import 'package:flutter/material.dart';


class OnlineProperty extends StatelessWidget {
  const OnlineProperty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
